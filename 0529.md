ViT视觉模型轻量化/实时推理，kv cache视频流或多模态模型里的缓存复用

> 面向直播/视频流的视觉大模型实时推理优化：减少每帧/每段视频的重复计算、降低延迟、降低显存和带宽开销。
>
> ViT 压缩主要针对单帧或单次输入中的视觉 token 冗余，例如剪枝、合并或量化不重要的 patch token；KV cache 则主要针对 Transformer 推理过程中的历史状态复用，最初常用于 LLM 自回归生成，但在视频流或多模态大模型中，连续帧和长视频同样会产生大量视觉 token 和历史上下文，因此也可以引入类似缓存复用与缓存压缩思想。二者结合后，可以面向直播场景形成“空间 token 压缩 + 时间缓存复用”的低延迟推理方案。
>
> ViT 压缩：加速视觉编码 / 视觉特征提取
> KV cache：加速大模型生成 / 长上下文推理

ViT / VLM / MLLM，本质上都在处理 **token 序列**. 

图像进 ViT 之后不是直接整张图处理，而是切成 patch：一帧图像 → patch tokens → Transformer attention → 输出分类/检测/理解结果

视频进入ViT后：多帧视频 → spatial tokens × temporal tokens → 超长 token 序列

- 直播的瓶颈：帧数多、分辨率高、token 多、attention 计算贵、显存占用大、实时性差
- ViT 压缩：哪些视觉 token 可以少算、跳过、合并、降精度
- KV cache：已经算过的 token 表示，后面能不能复用，不要每次重新算



ViT压缩类型：

1. 模型权重压缩：量化、剪枝、蒸馏、低秩分解、小模型替代大模型
2. Token pruning：删掉不重要 patch。
   1. 大量背景其实没必要每次都完整推理：墙面、地板、天空、桌子背景
   2. 有用的在于物品，需要调整视觉重心：人、手、物体、动作区域、变化区域
   3. DynamicViT（https://arxiv.org/abs/2106.02034）：ViT 中动态、渐进地剪掉冗余 token，只保留更重要的 token
   4. EViT（https://arxiv.org/abs/2202.07800）：根据 class token 的注意力识别重要 token，并对不重要 token 做重组织/融合；论文报告 DeiT-S 在 ImageNet 上可提升约 50% 推理速度，同时精度只下降约 0.3%
3. Token merging：不删 token，而是合并相似 token
   1. ToMe token merge（https://arxiv.org/abs/2210.09461）：不是直接丢弃 token，而是把相似 token 合并，优点是不需要重新训练，能直接加到已有 ViT 上；论文报告在图像、视频、音频 ViT 上都能提升吞吐，例如视频 ViT-L 可达到约 2.2 倍吞吐提升，精度下降很小
4. VLM / MLLM 的 visual token compression：把图片/视频编码成大量视觉 token，再送进语言模型。这里 visual token 数量会直接影响显存、延迟和吞吐。
   1. SparseVLM（https://arxiv.org/abs/2410.04417）：它提出 text-guided training-free token sparsification，用文本问题引导视觉 token 稀疏化；论文报告在 LLaVA 上可减少 54% FLOPs、降低 37% CUDA latency，同时保持 97% 原始准确率。
   2. ATP-LLaVA（https://openaccess.thecvf.com/content/CVPR2025/papers/Ye_ATP-LLaVA_Adaptive_Token_Pruning_for_Large_Vision_Language_Models_CVPR_2025_paper.pdf） 核心是根据输入样本自适应决定 pruning ratio，并加入空间增强 pruning 策略。
   3. FitPrune（https://arxiv.org/abs/2409.10197）强调 training-free，即不训练额外模块，直接快速剪掉冗余视觉 token。



kv cache用于直播：

1. 直播视频不是一次性输入完整视频，而是连续输入：帧1 帧2 帧3.相邻帧之间高度相似，并且背景没变、人物位置小幅变化、动作连续、语义上下文连续
2. 关键帧完整推理、中间帧复用历史视觉 token / KV cache、只对变化区域或新增 token 做更新
3. 优化：实时视频流输入 → 视觉模型推理 → 缓存复用 → 延迟下降 → 资源下降
4. XStreamVGGT（https://arxiv.org/abs/2602.21780） 面向 streaming 3D reconstruction，明确用了 KV cache compression，并指出多帧 patch tokens 存在大量冗余；在 cache length 2K/4K/6K/8K 的消融中，性能变化不大，说明多帧视觉 token 冗余明显。

---



```text
第一类：ViT / 视觉编码器压缩
目标：让视觉模型本身更快，比如 ViT、DeiT、Swin、CLIP vision encoder。

第二类：VLM / MLLM visual token compression
目标：减少送进 LLM 的视觉 token，主要优化多模态大模型的 prefill 阶段。

第三类：KV cache compression / reuse
目标：减少自回归生成时的缓存显存和读取开销，主要优化 LLM decoding 阶段。

第四类：视频流/直播场景的跨帧复用
目标：利用连续帧相似性，减少重复视觉计算，可以把 visual token compression 和 KV cache 思路结合起来。
```

#### 1. ViT / visual token compression 基础论文

###### 1. DynamicViT：动态 token 剪枝的经典基础

DynamicViT: Efficient Vision Transformers with Dynamic Token Sparsification

**核心思想**：ViT 并不是所有 patch token 都重要。模型最终分类往往只依赖少量关键 token，所以可以在中间层动态删掉不重要 token。

**作用位置**：ViT 的 Transformer block 中间层，主要作用于视觉编码器内部。

**具体做法**：

原始 ViT 是：

```text
patch tokens → block1 → block2 → ... → blockL → class token → 分类
```

DynamicViT 插入一个轻量预测模块：

```text
patch tokens → Transformer block → token importance predictor → prune tokens → 后续 block
```

它不是一次性剪，而是**分层、渐进式剪枝**：

```text
浅层剪一部分
中层再剪一部分
深层只保留最重要 token
```

它的关键难点是：直接删除 token 不可导。于是论文用了 **attention masking strategy**，通过阻断被剪 token 与其他 token 的 attention 交互，让剪枝过程可以端到端训练。

**近似算法流程**：

```text
For each pruning layer l:
    输入 token features X_l
    predictor 计算每个 token 的重要性分数 s_i
    按 keep ratio 选择 top-k tokens
    用 attention mask 屏蔽被剪 token 的交互
    继续送入后续 Transformer block
```

**结果**：论文报告在分层剪掉约 66% 输入 token 时，可以减少 31%–37% FLOPs，吞吐提升超过 40%，精度下降在 0.5% 以内。([arXiv](https://arxiv.org/abs/2106.02034?utm_source=chatgpt.com))

这个适合用来解释：

```text
直播画面中背景区域、静止区域、低变化区域可以少算
```

但 DynamicViT 需要训练或微调 predictor

------

###### 2. EViT：用 class token attention 找关键 patch

Expediting Vision Transformers via Token Reorganizations

**核心思想**：class token 最终负责分类，所以哪个 patch token 对 class token attention 高，哪个就更重要。低 attention token 不一定直接丢掉，可以融合成一个 token，避免信息完全丢失。

**作用位置**：ViT 的 MHSA 和 FFN 之间。

**具体结构**：

普通 ViT block：

```text
X → MHSA → FFN → X'
```

EViT 改成：

```text
X → MHSA → 根据 class attention 识别 attentive tokens
          → 保留重要 tokens
          → 融合不重要 tokens
          → FFN
```

**算法思想**：

```text
class token 对 patch token 的 attention = token 重要性
高 attention tokens：保留
低 attention tokens：融合成较少 token
```

这比纯 pruning 稳一点，因为背景信息不会完全丢失，而是被压缩进少量融合 token。

**结果**：EViT 论文提出在 ViT 前向过程中重组织 token，保留 attentive tokens，融合 inattentive tokens，从而加速后续 MHSA 和 FFN 计算。([arXiv](https://arxiv.org/abs/2202.07800?utm_source=chatgpt.com))

```text
人物、手部、物体区域 → attentive tokens
背景、墙面、地板 → inattentive tokens
```

然后只让模型重点处理 attentive tokens。

------

###### 3. ToMe：token merging，不训练、最适合快速做实验

Token Merging: Your ViT But Faster

**核心思想**：不是直接删 token，而是把相似 token 合并。

这点很重要。因为 pruning 是：

```text
token A, B, C, D → 保留 A, C，删除 B, D
```

ToMe 是：

```text
token A, B, C, D → A+B 合并，C+D 合并
```

也就是减少 token 数，同时尽量保留信息。

**作用位置**：ViT 的 Transformer block 中，逐层减少 token 数。

**具体做法**：

ToMe 大致流程：

```text
1. 计算 token 之间的相似度
2. 找到最相似的 token pairs
3. 把相似 token 按权重合并
4. 后续层处理更短的 token 序列
```

可以理解为：

```text
x_merge = weighted_average(x_i, x_j)
```

**优点**：

```text
不需要重新训练
可以直接插入已有 ViT
比直接删除 token 更温和
图像、视频、音频 ViT 都能用
```

**结果**：ToMe 在不训练的情况下，可以让 ViT-L / ViT-H 图像模型吞吐约 2 倍，视频 ViT-L 吞吐约 2.2 倍，精度下降只有约 0.2%–0.3%。([arXiv](https://arxiv.org/abs/2210.09461?utm_source=chatgpt.com))

你可以做：

```text
Baseline：原始 ViT/CLIP/DINOv2
Method：加 ToMe
比较：FPS、latency、GPU memory、输出稳定性
```

尤其是直播视频，因为连续帧里大量 token 很相似，ToMe 的直觉非常适合。

------

###### 4. Token Compression Meets Compact Vision Transformers：2025 综述 + 实证

Token Compression Meets Compact Vision Transformers: A Survey and Comparative Evaluation for Edge AI

**核心思想**：过去很多 token compression 论文都在 ViT-B、ViT-L 这种大模型上验证，但边缘设备/实时系统往往用 compact ViT。问题是：**token compression 对已经很小的 ViT 还有效吗？**

**作用位置**：不是提出单一算法，而是做 taxonomy 和 comparative evaluation。

**它关注的分类**：

```text
pruning：删 token
merging：合并 token
hybrid：剪枝 + 合并
fine-tuning-based：需要训练
plug-in：不训练即插即用
```

**重要结论**：论文认为 token compression 在通用大 ViT 上有效，但直接应用到 compact ViT 上时，效果经常下降，因为 compact ViT 本身已经做过结构压缩，冗余没那么多。([arXiv](https://arxiv.org/abs/2507.09702?utm_source=chatgpt.com))

这篇适合写相关工作里的“现实限制”：

```text
如果直播系统本来就用小模型，token compression 未必收益很大；
如果用大 ViT / VLM / MLLM，收益更明显。
```

------

###### 5. Patch Ranking / Efficient CLIP：CLIP 视觉 token 剪枝

Efficient CLIP by Learning to Rank Local Patches / Token Pruning as Ranking Prediction for Efficient CLIP

**核心思想**：CLIP 的 ViT backbone 也有大量冗余 patch。它们把 token pruning 变成一个 **ranking prediction** 问题。

**作用位置**：CLIP 的 vision encoder 内部。

**具体做法**：

```text
1. 用 greedy search 找到 patch token 的 Golden Ranking
2. 训练轻量 predictor 去近似这个 ranking
3. 推理时按 ranking 保留重要 patch
4. 用 learnable visual tokens 补偿剪枝造成的信息损失
```

**和 DynamicViT 的区别**：

```text
DynamicViT：一般 ViT 分类场景
Patch Ranking：CLIP 图文对齐场景
```

**结果**：该工作系统研究了 CLIP ViT backbone 的 token pruning，并提出 Golden Ranking + lightweight predictor 的方法。([Pedro Morgado](https://pedro-morgado.github.io/publications/2025-token-pruning.html?utm_source=chatgpt.com))

```text
画面 → CLIP image encoder
候选文本：
a person is walking
a person is sitting
a person is using a phone
```

```text
CLIP token pruning 后，图文匹配结果是否稳定？
```

------

##### 2. VLM / MLLM visual token compression 新论文

```text
image / video
   ↓
vision encoder, usually ViT / CLIP / SigLIP
   ↓
visual tokens
   ↓
projector / connector
   ↓
LLM
   ↓
answer
```

visual token compression 可以发生在三个地方：

```text
1. vision encoder 内部
2. vision encoder 输出后、进入 projector 前
3. projector 后、进入 LLM 前
```

------

###### 6. FitPrune：training-free MLLM visual token pruning

Fit and Prune: Fast and Training-free Visual Token Pruning for Multi-modal Large Language Models

**核心思想**：不要训练额外模块，而是把 MLLM token pruning 看成一个统计优化问题：剪枝前后 attention distribution 尽量接近。

**作用位置**：MLLM 中进入 LLM 的视觉 token。

**具体做法**：

它的目标可以理解成：

```text
找到一个 pruning scheme
让 pruning 后的 attention distribution
尽量接近原始 full-token attention distribution
```

也就是最小化：

```text
D(Attention_full || Attention_pruned)
```

其中 `D` 可以理解为分布差异，比如 KL divergence 这类思想。

**算法流程**：

```text
1. 给定压缩预算 budget
2. 分析原模型 attention distribution
3. 搜索/拟合一套 pruning recipe
4. 推理时按 recipe 删除冗余视觉 token
```

**结果**：FitPrune 是 AAAI 2025 工作，强调 fast、training-free、可以快速生成满足预定义 budget 的 pruning recipe。([AAAI Publications](https://ojs.aaai.org/index.php/AAAI/article/view/34366?utm_source=chatgpt.com))

```text
采用 training-free visual token pruning，避免重新训练大规模 MLLM，只比较不同 token budget 下的延迟与输出质量。
```

------

###### 7. SparseVLM：用文本问题指导视觉 token 稀疏化

SparseVLM: Visual Token Sparsification for Efficient Vision-Language Model Inference

**核心思想**：视觉 token 是否重要，应该和用户问题有关。

例如同一张图：

```text
问题1：这个人在做什么？
重要 token：人、手、物体

问题2：背景是什么地方？
重要 token：房间、墙面、家具
```

所以 SparseVLM 用 text tokens 来指导 visual tokens 的选择。

**作用位置**：VLM / LVLM 推理过程中的视觉 token。

**具体做法**：

```text
1. 从 text tokens 中选择相关文本 token
2. 用 self-attention 矩阵衡量 visual token 对文本推理的贡献
3. 每层自适应决定 sparsification ratio
4. 被剪 token 不完全丢弃，而是 token recycling，压缩成更紧凑表示
```

**架构层次**：

```text
visual tokens + text tokens
        ↓
attention analysis
        ↓
text-guided visual token scoring
        ↓
prune / recycle
        ↓
LLM reasoning
```

**结果**：SparseVLM 在 LLaVA 上报告可减少 54% FLOPs、降低 37% CUDA latency，同时保持 97% 原始准确率。([arXiv](https://arxiv.org/abs/2410.04417?utm_source=chatgpt.com))

```text
问题：当前画面中有没有异常行为？
```

那模型应该关注人物和动作区域，而不是背景。SparseVLM 的思路就很适合：

```text
任务/问题引导的视觉 token 压缩
```

------

###### 8. ATP-LLaVA：层级 + 样本自适应 token pruning

ATP-LLaVA: Adaptive Token Pruning for Large Vision Language Models

**核心思想**：不同样本、不同 LLM layer 对视觉 token 的需求不一样，所以不能用固定 pruning ratio。

**作用位置**：LLaVA 这类 LVLM 的 LLM decoder 层中。

**具体做法**：

传统方法可能是：

```text
每层都保留 50% visual tokens
```

ATP-LLaVA 是：

```text
图像-问题简单 → 多剪一点
图像-问题复杂 → 少剪一点
浅层需要更多视觉信息 → 少剪
深层信息已经聚合 → 多剪
```

**架构层次**：

```text
visual tokens
   ↓
LLM layer 1: adaptive keep ratio
   ↓
LLM layer 2: adaptive keep ratio
   ↓
...
   ↓
answer
```

**结果**：CVPR 2025 版本报告平均 token 数减少 75%，七个常用 benchmark 平均性能只下降约 1.9%。([CVF开放获取](https://openaccess.thecvf.com/content/CVPR2025/papers/Ye_ATP-LLaVA_Adaptive_Token_Pruning_for_Large_Vision_Language_Models_CVPR_2025_paper.pdf?utm_source=chatgpt.com))

“自适应策略”：

```text
直播中画面简单时提高压缩率；
画面复杂或动作变化大时降低压缩率。
```

------

###### 9. DivPrune：用 diversity 选择代表性 visual tokens

DivPrune: Diversity-based Visual Token Pruning for Large Multimodal Models

**核心思想**：只看重要性不够，因为选出来的 token 可能彼此很相似。应该选一组既重要又多样的 token，让它们尽可能代表原图/视频。

**作用位置**：LMM / MLLM 的 visual token selection。

**具体做法**：

它把 token pruning 形式化成：

```text
Max-Min Diversity Problem, MMDP
```

直觉是：

```text
选择一个 token 子集 S
让 S 内部 token 尽可能多样
同时能代表原始 token 集合
```

可以简单理解为：

```text
不要只选 10 个都在人的脸附近的 token
而是脸、手、物体、背景关键点都保留一些
```

**算法层次**：

```text
visual token features
   ↓
计算 token 间距离/相似度
   ↓
求解多样性最大子集
   ↓
保留代表性 tokens，剪掉其余 tokens
```

**结果**：DivPrune 是 CVPR 2025 工作，论文称在 16 个图像/视频语言数据集上达到 SOTA accuracy，并减少端到端延迟和 GPU memory。([arXiv](https://arxiv.org/abs/2503.02175?utm_source=chatgpt.com))

直播画面里如果只按 attention 选 token，可能总选人物中心，忽略手上物体。DivPrune 适合解决：

```text
保留 token 不够多样
导致细节丢失
```

------

###### 10. VisionZip：保留重要 token + 合并冗余 token

VisionZip: Longer is Better but Not Necessary in Vision Language Models

**核心思想**：VLM 现在倾向于使用越来越长的视觉 token，但很多 token 冗余。VisionZip 不只是剪，而是“选重要 token + 压缩剩余 token”。

**作用位置**：vision encoder 输出后、进入 LLM 前。

**具体做法**：

```text
1. 识别 informative tokens
2. 保留高信息 token
3. 对剩余 token 做聚类/合并
4. 把更短的 token 序列输入 LLM
```

它有点像：

```text
EViT + ToMe 在 VLM 场景里的综合版
```

**结果**：论文报告 VisionZip 在几乎所有设置下比之前 SOTA 至少提升 5%，prefill time 提升 8 倍，并让 LLaVA-Next-13B 推理速度快于 LLaVA-Next-7B，同时效果更好。([arXiv](https://arxiv.org/abs/2412.04467?utm_source=chatgpt.com))

```text
减少进入 LLM 的视觉 token
降低 prefill 延迟
```

prefill 阶段就是每次送一帧/多帧画面给 MLLM 的主要开销之一。

------

###### 11. FrameFusion：视频 token reduction，特别适合你

FrameFusion: Combining Similarity and Importance for Video Token Reduction on Large Visual Language Models

**核心思想**：视频里不只是单帧内部 token 冗余，帧与帧之间也高度相似。仅按重要性 pruning 不够，还应该先合并相似 token，再剪枝。

**作用位置**：视频 LVLM 的 visual tokens。

**具体做法**：

```text
1. 分析视频 token 相似度
2. 先 similarity-based merging：合并相似 token
3. 再 importance-based pruning：剪掉不重要 token
4. 把压缩后的视频 token 输入 LLM
```

也就是：

```text
视频 token 压缩 = 相似性合并 + 重要性剪枝
```

**结果**：FrameFusion 在 LLaVA-Video-7B/32B/72B 和 MiniCPM-V-8B 等模型上评估，报告减少 70% vision tokens，LLM 速度提升 3.4–4.4 倍，端到端速度提升 1.6–1.9 倍，平均性能影响小于 3%。([arXiv](https://arxiv.org/abs/2501.01986?utm_source=chatgpt.com))

**对你项目的启发**：

这篇是我最建议你重点看的之一。因为你现在不是静态图，而是直播流。你可以直接把它作为你的核心相关工作：

```text
直播连续帧存在相似性
所以 token reduction 不能只做单帧 pruning
还要做跨帧相似 token merging
```

------

###### 12. TokenPacker：压缩 projector，而不是只做 pruning

TokenPacker: Efficient Visual Projector for Multimodal LLM

**核心思想**：MLLM 的 visual projector 通常是简单 MLP，把视觉 token 一对一映射进 LLM。但是这种做法保留了太多冗余视觉 token。TokenPacker 认为 projector 本身应该负责把高分辨率视觉信息压缩成少量高质量 token。

**作用位置**：vision encoder 和 LLM 之间的 projector / connector。

**具体结构**：

```text
high-res visual features
        ↓
coarse low-resolution point query
        ↓
region-to-point injection
        ↓
condensed visual tokens
        ↓
LLM
```

它不是简单删 token，而是做 coarse-to-fine 信息注入：

```text
低分辨率 query 提供整体语义
高分辨率区域特征补充局部细节
最终得到少量但信息丰富的 tokens
```

**结果**：TokenPacker 报告可压缩 75%–89% visual tokens，同时在多种 benchmark 上达到相当甚至更好的性能。([arXiv](https://arxiv.org/abs/2407.02392?utm_source=chatgpt.com))

如果你想做“结构改造型”方案，可以考虑 projector 压缩，而不是单纯在外面剪 token。

但是这类方法通常需要训练，不如 ToMe / VisionZip / FrameFusion 快速。

------

###### 13. FastVLM：不要只剪 token，也可以重设计视觉编码器

**论文全名**：FastVLM: Efficient Vision Encoding for Vision Language Models

**核心思想**：VLM 的效率瓶颈不仅在 LLM，也在高分辨率视觉编码器。与其在后面剪 token，不如设计更高效的 vision encoder，让它天然输出更少 token、更快编码。

**作用位置**：VLM 的视觉编码器。

**具体做法**：

```text
高分辨率输入
   ↓
FastViTHD hybrid vision encoder
   ↓
更少 visual tokens
   ↓
LLM
```

它不是典型 token pruning，而是：

```text
优化视觉编码器结构 + 控制输出 token 数量
```

**结果**：FastVLM 报告在 LLaVA-1.5 setup 下 TTFT 提升 3.2 倍；与高分辨率 LLaVA-OneVision 相比，在相同 0.5B LLM 设置下，FastVLM 的 TTFT 快 85 倍，视觉编码器小 3.4 倍，并保持可比性能。([arXiv](https://arxiv.org/abs/2412.13303?utm_source=chatgpt.com))

```text
算法级压缩：pruning / merging
结构级压缩：换轻量 vision encoder
系统级压缩：缓存复用 / 关键帧策略
```

FastVLM 就属于结构级压缩。

------

###### 14. ZipVL：同时优化 prefill 和 decoding，很适合连接 visual token 与 KV cache

ZipVL: Efficient Large Vision-Language Models with Dynamic Token Sparsification

**核心思想**：LVLM 有两个瓶颈：

```text
prefill 阶段：视觉 token 太多，attention 计算慢
decode 阶段：KV cache 太大，读取慢、显存大
```

ZipVL 同时处理这两个阶段。

**作用位置**：

```text
1. prefill 阶段：只对重要 token 做 sparse attention
2. decode 阶段：丢弃不重要 token，减少 KV cache
```

**具体做法**：

```text
1. 根据各层 attention score 分布，自适应分配 sparsity ratio
2. 选择 normalized attention score 高的重要 token
3. prefill 只处理重要 token
4. 不重要 token 不进入 KV cache，降低 decode memory bottleneck
```

**结果**：ZipVL 在 LLaVA-Next-13B 上报告 prefill 加速 2.3 倍、decoding throughput 提升 2.8 倍，VQAv2 上准确率只下降约 0.5%。([arXiv](https://arxiv.org/abs/2410.08584?utm_source=chatgpt.com))



```text
visual token compression
+
KV cache reduction
```

你可以把它作为“ViT 压缩和 KV cache 为什么能放在一起”的直接证据。

------

###### 15. ForestPrune：2026 视频 MLLM 高比例 token 压缩

**论文全名**：ForestPrune: High-ratio Visual Token Compression for Video Multimodal Large Language Models via Spatial-Temporal Forest Modeling

**核心思想**：视频 MLLM 的 token 压缩不能只看单帧，还要建模时空连续性。ForestPrune 把视频 token 构造成跨帧的 spatial-temporal token forest。

**作用位置**：Video MLLM 的视频 visual tokens。

**具体做法**：

```text
1. 基于语义、空间、时间约束，在帧间构建 token forests
2. 一个 tree 表示跨帧连续的相似/相关视觉区域
3. 根据 tree depth 和 node role 评估重要性
4. 做全局 pruning 决策
```

**为什么叫 forest**：

```text
视频中不同物体/区域形成不同 token tree
多个 tree 组成 forest
```

**结果**：ForestPrune 是 2026 arXiv 新工作，报告在 LLaVA-OneVision 上减少 90% tokens 仍保留 95.8% average accuracy；在 LLaVA-Video 上相比 FrameFusion，MLVU 准确率高 10.1%，pruning time 少 81.4%。([arXiv](https://arxiv.org/abs/2603.22911?utm_source=chatgpt.com))

**对你项目的启发**：

这是你做直播方向时非常新的论文。你可以借鉴它的思想：

```text
直播视频流不是独立帧序列，而是连续时空结构；
可以按区域/物体轨迹维护 token tree 或 token cache。
```

------

##### 3. KV cache compression 论文

KV cache 的核心问题是：

```text
LLM 每生成一个 token，都需要访问历史 token 的 K/V
上下文越长，KV cache 越大
decode 阶段就越慢、越占显存
```

KV cache 大小大致和下面成正比：

```text
KV cache memory ∝ 2 × layers × seq_len × kv_heads × head_dim × bytes
```

其中 `2` 是因为有 Key 和 Value。

对 MLLM 来说，visual tokens 也会进入上下文，所以：

```text
视觉 token 越多 → prefill 越慢 → KV cache 越大 → decode 越慢
```

这就是 KV cache 和视觉模型的关系。

------

###### 16. H2O：保留 heavy hitter tokens

**论文全名**：H2O: Heavy-Hitter Oracle for Efficient Generative Inference of Large Language Models

**核心思想**：注意力不是均匀分布的，少量 token 对 attention 贡献很大，这些 token 叫 Heavy Hitters。保留这些 token，再保留最近 token，就可以减少 cache。

**作用位置**：LLM decoding 阶段的 KV cache eviction。

**具体做法**：

传统 full cache：

```text
保留所有历史 token 的 KV
```

H2O：

```text
保留 heavy hitter tokens
+
保留 recent tokens
+
淘汰其他 tokens
```

**算法直觉**：

```text
重要历史 token：保证长程依赖
最近 token：保证局部连贯
```

**结果**：H2O 提出基于 heavy hitter 的 KV cache eviction policy，动态保留 recent tokens 和 heavy hitter tokens，以减少 KV cache footprint。([arXiv](https://arxiv.org/abs/2306.14048?utm_source=chatgpt.com))

**对你项目的启发**：

如果放到视频流：

```text
heavy hitter visual tokens = 长时间被关注的关键区域
recent visual tokens = 最近几帧的新增变化
```

这可以迁移成“直播视觉缓存策略”。

------

###### 17. StreamingLLM：attention sink + sliding window

**论文全名**：Efficient Streaming Language Models with Attention Sinks

**核心思想**：只保留最近窗口会失败，因为模型会强烈关注初始 token，即 attention sink。只要保留初始 sink tokens + 最近窗口，就能支持无限长流式输入。

**作用位置**：流式 LLM decoding 的 KV cache。

**具体做法**：

```text
保留开头几个 attention sink tokens
+
保留最近 W 个 tokens
+
丢弃中间旧 tokens
```

结构上是：

```text
[sink tokens] + [sliding window recent tokens]
```

**结果**：StreamingLLM 可让 Llama-2、MPT、Falcon、Pythia 稳定处理最多 400 万 token 级别的流式文本；在 streaming 设置中，相比 sliding window recomputation baseline 最高 22.2 倍加速。([arXiv](https://arxiv.org/abs/2309.17453?utm_source=chatgpt.com))

直播场景也可以类比：

```text
sink tokens：场景全局背景、任务 prompt、初始参考帧
recent tokens：最近几帧变化
```

这就是“视觉直播流缓存”的一个非常好的理论类比。

------

###### 18. KIVI：2-bit KV cache 量化

**论文全名**：KIVI: A Tuning-Free Asymmetric 2bit Quantization for KV Cache

**核心思想**：KV cache 太占显存，可以低比特量化。但 Key 和 Value 的分布不同，所以要非对称量化：

```text
Key cache：per-channel quantization
Value cache：per-token quantization
```

**作用位置**：LLM KV cache 存储。

**具体做法**：

```text
K 按 channel 分组量化
V 按 token 分组量化
量化到 2 bit
```

**为什么 K 和 V 不一样**：

论文分析发现 Key cache 的异常值/分布更适合 channel 维度处理，而 Value cache 更适合 token 维度处理。([arXiv](https://arxiv.org/abs/2402.02750?utm_source=chatgpt.com))

**结果**：KIVI 报告在 Llama、Falcon、Mistral 上质量几乎不变，峰值显存降低 2.6 倍，支持最多 4 倍 batch size，真实推理吞吐提升 2.35–3.47 倍。([arXiv](https://arxiv.org/abs/2402.02750?utm_source=chatgpt.com))



如果你最后用 Qwen-VL / LLaVA / InternVL 这类模型，KV cache 量化可以降低显存压力。

但如果你只是用 ViT/CLIP 做视觉特征提取，KIVI 这种 LLM KV cache 技术就不直接适用。

------

###### 19. KVQuant：面向超长上下文的 KV cache 量化

**论文全名**：KVQuant: Towards 10 Million Context Length LLM Inference with KV Cache Quantization

**核心思想**：KIVI 是 2-bit KV cache 的重要工作，KVQuant 更系统地处理 sub-4-bit KV quantization，尤其关注超长上下文。

**作用位置**：LLM 长上下文推理的 KV cache。

**关键技术**：

```text
1. Per-Channel Key Quantization
2. Pre-RoPE Key Quantization
3. Non-Uniform KV Cache Quantization
4. Per-Vector Dense-and-Sparse Quantization
```

重点解释一下：

```text
Pre-RoPE Key Quantization：
在旋转位置编码 RoPE 之前量化 Key，减少 RoPE 对量化分布的破坏。

Dense-and-Sparse Quantization：
普通值用低比特量化，异常值单独处理，避免 outlier 拉大量化范围。
```

**结果**：KVQuant 报告 3-bit quantization 在 Wikitext-2 和 C4 上 perplexity degradation 小于 0.1；LLaMA-7B 在单张 A100-80GB 上可支持最高 100 万上下文，8 GPU 可支持最高 1000 万上下文，并用 CUDA kernel 获得最高约 1.7 倍加速。([arXiv](https://arxiv.org/abs/2401.18079?utm_source=chatgpt.com))

是“直播长时间持续运行”，KVQuant 这类方法可以作为：

```text
长上下文/长时间缓存压缩
```

------

###### 20. PyramidKV：不同层分配不同 cache budget

**论文全名**：PyramidKV: Dynamic KV Cache Compression based on Pyramidal Information Funneling

**核心思想**：LLM 层间信息流不是均匀的。低层 attention 分散，需要更多 cache；高层信息聚合，可以保留更少 cache。于是 cache budget 应该按层动态分配。

**作用位置**：LLM 每一层的 KV cache。

**具体做法**：

传统做法：

```text
每层保留同样数量的 KV cache
```

PyramidKV：

```text
低层：保留更多 KV
中层：适中
高层：保留更少 KV
```

形状像金字塔，所以叫 PyramidKV。

**核心观察**：

```text
lower layers：attention scattering widely
higher layers：attention focuses on critical tokens
```

**结果**：PyramidKV 在 LongBench 上报告只保留 12% KV cache 就能接近 full cache；极端情况下只保留 0.7% KV cache，在 TREC 上比其他压缩方法高最多 20.5 absolute accuracy。([arXiv](https://arxiv.org/abs/2406.02069?utm_source=chatgpt.com))

可以迁移成视觉模型里的层级压缩：

```text
浅层视觉 token 保留多一些，因为局部细节还没聚合；
深层视觉 token 可以压得更狠，因为语义已经形成。
```

这和 ATP-LLaVA 的层级自适应思想也能对应。

------

###### 21. ZigZagKV：按 layer uncertainty 分配预算

**论文全名**：ZigZagKV: Dynamic KV Cache Compression for Long-context Modeling based on Layer Uncertainty

**核心思想**：不同层需要的最小 cache budget 不同，应该根据层的不确定性分配 KV cache，而不是固定每层比例。

**作用位置**：LLM 每层 KV cache。

**具体做法**：

```text
1. 估计每层 attention / hidden state output 的不确定性
2. 不确定性高的层保留更多 cache
3. 不确定性低的层保留更少 cache
```

**和 PyramidKV 的区别**：

```text
PyramidKV：基于信息漏斗模式，低层多、高层少
ZigZagKV：基于 layer uncertainty，更动态，不一定严格金字塔
```

**结果**：ZigZagKV 报告把 KV cache memory 降到 full KV 的约 20%，同时接近无损性能。([arXiv](https://arxiv.org/abs/2412.09036?utm_source=chatgpt.com))

自适应压缩，可以用这个思想：

```text
画面稳定、输出确定 → 压缩更强
画面变化大、模型不确定 → 保留更多 token/cache
```

------

###### 22. SnapKV：用 observation window 预测重要 KV

**论文全名**：SnapKV: LLM Knows What You are Looking for Before Generation

**核心思想**：LLM 在生成前，其实已经能从 prompt 末尾的一小段 observation window 看出后续会关注哪些历史 token。于是可以在生成前压缩 KV cache。

**作用位置**：prefill 后、decode 前的 KV cache 压缩。

**具体做法**：

```text
1. 在 prompt 末尾取 observation window
2. 观察每个 attention head 关注哪些历史位置
3. 对重要 KV positions 聚类选择
4. 每个 head 保留自己的关键 KV
5. decode 阶段只用压缩后的 KV cache
```

**结果**：SnapKV 报告在 16K tokens 输入下，相比 baseline 生成速度提升 3.6 倍，memory efficiency 提升 8.2 倍；在单张 A100-80GB 上可处理最高 380K context tokens，Needle-in-a-Haystack 只有很小准确率下降。([arXiv](https://arxiv.org/abs/2404.14469?utm_source=chatgpt.com))

直播视频里也可以有 observation window：

```text
最近几帧/最近几秒作为 observation window
判断哪些区域/token 后续值得缓存
```

这非常适合做“视频流 KV/token cache 预测”。

------

###### 23. Quest：query-aware KV page selection

**论文全名**：Quest: Query-Aware Sparsity for Efficient Long-Context LLM Inference

**核心思想**：token 是否重要不是固定的，而取决于当前 query。Quest 不永久保留固定 token，而是在当前 query 下选择最相关的 KV cache pages。

**作用位置**：LLM self-attention 读取 KV cache 的阶段。

**具体做法**：

```text
1. 把 KV cache 分页
2. 每页保存 Key 的 min/max 统计
3. 当前 query 到来时，估计每页重要性
4. 只加载 Top-K critical KV pages
5. 对这些 page 做 attention
```

**好处**：

```text
不是减少 cache 存储本身，而是减少每次 attention 需要加载的 cache
```

**结果**：Quest 报告 self-attention speedup 最高 2.23 倍，推理延迟降低最高 7.03 倍，在长依赖任务上准确率损失很小。([arXiv](https://arxiv.org/abs/2406.10774?utm_source=chatgpt.com))

直播问答中，不同问题关注不同区域：

```text
“人在做什么？” → 人体动作 tokens
“桌上有什么？” → 桌面物体 tokens
```

这和 SparseVLM 一样，都是 query-aware 思想。

------

###### 24. MiniCache：跨层 KV cache 合并

**论文全名**：MiniCache: KV Cache Compression in Depth Dimension for Large Language Models

**核心思想**：相邻层，尤其是中深层的 KV states 很相似，所以可以从 depth dimension 压缩 KV cache，而不是只沿 token 维度压缩。

**作用位置**：LLM 不同层之间的 KV cache。

**具体做法**：

```text
1. 发现相邻层 KV states 相似
2. 把状态拆成 magnitude 和 direction
3. direction 插值合并
4. magnitude 保持不变
5. 对差异大的 token pairs 保留，不强行合并
```

**和 H2O/PyramidKV 的区别**：

```text
H2O：沿 token 维度淘汰
PyramidKV：沿 layer 维度分配不同预算
MiniCache：直接利用相邻 layer KV 相似性做合并
```

**结果**：MiniCache 在 LLaMA-2、LLaMA-3、Phi-3、Mistral、Mixtral 等模型上评估；ShareGPT 上 LLaMA-2-7B 使用 4-bit MiniCache 可达到最高 5.02 倍压缩比，吞吐约 5 倍提升，显存相比 FP16 full cache 降低 41%，且接近无损。([arXiv](https://arxiv.org/abs/2405.14366?utm_source=chatgpt.com))

这个可以作为“缓存不只按时间/token 压，也可以按层压”的补充。

------

###### 25. RocketKV：两阶段 KV cache 压缩

**论文全名**：RocketKV: Accelerating Long-Context LLM Inference via Two-stage KV Cache Compression

**核心思想**：单阶段 KV 压缩不够稳。RocketKV 把压缩拆成两阶段：

```text
1. permanent KV token eviction
2. dynamic KV token selection
```

**作用位置**：长上下文 LLM decode 阶段。

**具体做法**：

```text
第一阶段：粗粒度永久淘汰一部分 KV
第二阶段：每次 decode 时动态选择当前需要的 KV
```

论文中第一阶段可以直接采用 SnapKV 做 coarse-grain permanent eviction。([arXiv](https://arxiv.org/html/2502.14051v3?utm_source=chatgpt.com))

直播场景也可以设计两阶段：

```text
长期缓存：只保留场景/对象级关键 token
短期缓存：根据当前帧/当前问题动态选择最近 token。
```

------

###### 26. TurboQuant：2026 低比特 KV cache 新方法

**论文/方法**：TurboQuant

**核心思想**：用更适合向量压缩的方式压 KV cache，目标是在低比特下减少误差和额外存储开销。

**主要技术**：

```text
1. PolarQuant：把向量从 Cartesian 坐标转成 polar 坐标
2. QJL：Quantized Johnson-Lindenstrauss，1-bit error correction
```

**作用位置**：LLM KV cache quantization。

**结果**：公开报道显示，TurboQuant 是 training-free KV cache compression，可将 KV cache 压到 3-bit，并在 H100 上带来最高 8 倍性能提升，KV memory 至少减少 6 倍；报道称其会在 ICLR 2026 展示。这个目前我建议作为“最新趋势”引用，而不是作为你实验的主要依据，因为最好等正式论文和代码更稳定。([Tom's Hardware](https://www.tomshardware.com/tech-industry/artificial-intelligence/googles-turboquant-compresses-llm-kv-caches-to-3-bits-with-no-accuracy-loss?utm_source=chatgpt.com))

------

### AI补的一批新的论文方向

###### A. 视觉 / MLLM token compression 新论文

| 论文        | 年份      | 主要思想                                                   | 作用位置                 | 适合你吗             |
| ----------- | --------- | ---------------------------------------------------------- | ------------------------ | -------------------- |
| VisionZip   | 2024/2025 | 重要 token 保留 + 冗余 token 合并                          | 视觉 token 进入 LLM 前   | 很适合               |
| FrameFusion | 2025      | 视频 token 相似性合并 + 重要性剪枝                         | Video LVLM visual tokens | 非常适合             |
| TokenPacker | 2024/2025 | 重设计 visual projector，用少量 token 保留细节             | projector / connector    | 适合写相关工作       |
| FastVLM     | 2024/2025 | 高效视觉编码器，减少编码延迟和 token 数                    | vision encoder           | 适合直播实时性       |
| ZipVL       | 2024/2025 | 同时优化 prefill 和 decode 的 visual token / KV bottleneck | LVLM inference           | 非常适合连接两条线   |
| ForestPrune | 2026      | 视频时空 token forest，高比例压缩                          | Video MLLM               | 很新，适合展望       |
| SVD-Prune   | 2026      | 用 SVD leverage score 做 training-free token pruning       | VLM visual token         | 很新，适合补充       |
| FastAV      | 2026      | 面向音视频 LLM 的 token pruning                            | audio-visual LLM         | 如果你扩展多模态可提 |

其中 SVD-Prune 把 visual token feature matrix 做 SVD，用 leverage scores 选 top-k token，目标是在极低视觉 token budget 下保留主导全局方差信息。([arXiv](https://arxiv.org/pdf/2604.11530?utm_source=chatgpt.com)) FastAV 则是面向 audio-visual LLM 的 token pruning，说明 token pruning 已经从纯 VLM 扩展到音视频多模态 LLM。([arXiv](https://arxiv.org/html/2601.13143v1?utm_source=chatgpt.com))

------

###### B. KV cache 新论文 / 新方向

| 论文         | 年份      | 主要思想                               | 作用位置                            | 适合你吗         |
| ------------ | --------- | -------------------------------------- | ----------------------------------- | ---------------- |
| StreamingLLM | 2023/2024 | attention sink + sliding window        | 流式 LLM                            | 很适合类比直播   |
| SnapKV       | 2024      | observation window 预测重要 KV         | prefill 后 decode 前                | 很适合           |
| Quest        | 2024      | query-aware KV page loading            | decode attention                    | 适合问答型直播   |
| MiniCache    | 2024      | 跨层 KV 合并                           | layer/depth dimension               | 适合补充         |
| RocketKV     | 2025      | 永久淘汰 + 动态选择两阶段              | long-context decode                 | 适合系统设计     |
| PureKV       | 2025      | 不依赖显式 attention matrix 的 KV 优化 | FlashAttention/SparseAttention 兼容 | 适合最新趋势     |
| SnapStream   | 2025      | SnapKV + StreamingLLM rolling cache    | 生产级长上下文                      | 适合系统相关工作 |
| TurboQuant   | 2026      | PolarQuant + QJL 低比特 KV 量化        | KV quantization                     | 很新，谨慎引用   |

PureKV 的出发点是：很多 KV pruning 依赖 attention score，但 FlashAttention / Sparse Attention 不显式输出完整 attention matrix，所以它关注与高效 attention 实现兼容的 KV 优化。([arXiv](https://arxiv.org/html/2510.25600v1?utm_source=chatgpt.com)) SnapStream 则把 SnapKV 的 prefill 压缩和 StreamingLLM 的 rolling KV window 结合，用于生产级长上下文部署。([arXiv](https://arxiv.org/html/2511.03092v3?utm_source=chatgpt.com))

------

##### 5. 这些方法到底分别优化哪个阶段？

| 阶段                 | 输入                      | 瓶颈                   | 对应方法                                              |
| -------------------- | ------------------------- | ---------------------- | ----------------------------------------------------- |
| 视觉编码器阶段       | 图像 patch / 视频帧 patch | ViT attention 计算贵   | DynamicViT、EViT、ToMe、FastVLM                       |
| 视觉 token 入 LLM 前 | visual tokens             | token 太多，prefill 慢 | VisionZip、SparseVLM、FitPrune、DivPrune、TokenPacker |
| 视频多帧阶段         | 多帧 visual tokens        | 帧间冗余大             | FrameFusion、ForestPrune                              |
| LLM prefill 阶段     | prompt + visual tokens    | 一次性处理长上下文慢   | ZipVL、VisionZip、FrameFusion                         |
| LLM decode 阶段      | KV cache                  | 显存大、cache 读取慢   | H2O、KIVI、KVQuant、PyramidKV、SnapKV、Quest          |
| 长时间流式阶段       | 持续输入                  | cache 无限增长         | StreamingLLM、SnapStream、RocketKV                    |

------

## 

```text
1. ToMe 最容易复现，适合做 ViT token merging baseline
2. VisionZip VLM/MLLM 视觉 token 压缩代表
3. FrameFusion 视频 token reduction，和直播最相关
4. ZipVL 同时连接 visual token 和 KV cache
5. SnapKV 用 observation window 做缓存压缩，很适合视频流类比
6. StreamingLLM 流式缓存机制，适合直播系统叙事

相关工作和扩展创新点
7. DynamicViT
8. EViT
9. SparseVLM
10. ATP-LLaVA
11. DivPrune
12. PyramidKV
13. KIVI
14. KVQuant

最新趋势
15. ForestPrune
16. SVD-Prune
17. RocketKV
18. PureKV
19. TurboQuant
20. FastAV
```

------



>  ViT 压缩主要解决视觉编码器中的 patch token 冗余问题，比如通过 token pruning、token merging 或 projector compression 减少视觉计算；KV cache 压缩主要解决 LLM 或 MLLM decoding 阶段历史 Key/Value 缓存过大导致的显存和读取开销问题。二者看似分别属于视觉模型和语言模型，但在 VLM/MLLM 和视频理解场景中会连接起来：视频帧会产生大量 visual tokens，这些 token 进入 LLM 后不仅增加 prefill 计算，也会扩大后续 KV cache。因此可以把研究方向统一为“面向直播视频流的视觉 token 压缩与缓存复用推理优化”，重点优化端到端延迟、显存、吞吐和输出稳定性，而不是继续训练动作识别模型追求单一准确率。

> 直播视频流中连续帧冗余很高，因此不再逐帧完整调用视觉模型，而是采用“关键帧完整推理 + 中间帧缓存复用 + 视觉 token 压缩”的方式降低端到端延迟。不是提高模型识别准确率，而是在尽量保持输出稳定性的前提下，降低直播分析系统的推理延迟、显存占用和计算量。

第一层：直播视频输入层: 负责采样、分辨率调整、关键帧选择、帧缓存

第二层：视觉理解层: 使用 ViT / VLM / MLLM 进行画面识别、动作理解或异常分析

第三层：推理优化层: 使用 token pruning、token merging、KV cache、关键帧复用降低延迟

---

做法1 在直播时直接做视觉Token的压缩推理和优化：

```
输入视频流，使用摄像头或者视频传输
每隔 N 帧取关键帧
用视觉模型（ViT、CLIP、DINOv2） 提取视觉特征
加入 token pruning或token merging
比较不同压缩率下的延迟、吞吐、显存、输出稳定性
```

指标：

```
avg_latency_ms
p95_latency_ms
FPS
GPU memory
token_keep_ratio
semantic_consistency
```

---

做法2 实时视频流的视觉 Token 压缩与跨帧cache复用：

```
关键帧完整推理
非关键帧只处理变化区域
相似视觉token跨帧合并
重要token保留，不重要token压缩
未变化区域复用上一帧 visual tokens / features / KV cache
再结合 token pruning 减少每帧 token 数
动态调整压缩率和视频帧率
关键帧 + 增量帧 + 缓存更新
```

---

做法3 Token 与 KV Cache 联合压缩：

```
Video-LLaVA / Qwen2.5-VL / InternVL
输入多帧视频
减少 visual tokens
压缩 LLM KV cache
比较回答延迟和显存
```



---

### 矛盾点：

“时序窗口必须足够长才能识别动作”和“窗口越长延迟越高”之间的矛盾

token量和chunk 怎么攒、攒多长、多久出一次结果、延迟能不能接受的矛盾

我现在攒够多少帧了？这段时间够不够判断一个动作？多久输出一次结果？输出后是不是还要等下一个 chunk？

token 量的意义在于：**一个 chunk 送进模型以后，模型处理这段 chunk 有多慢。**

也就是第二阶段延迟：总延迟 = 攒 chunk 的等待时间 + 模型推理时间 + 传输/渲染时间

```
攒 chunk 的等待时间：由 clip_len / ai_fps 决定
模型推理时间：由模型大小、token 数、分辨率、帧数决定
```

流式chunk识别：那就采用类kv cache的思想了

```
滑动窗口
持续缓存最近 N 帧
每隔 stride 帧推理一次
输出平滑结果
```

例如：

```
最近 16 帧组成一个 chunk
每来 4 帧更新一次预测
```

流程是：

```
t = 0: 还没攒够，不输出
t = 16: 第一次输出
t = 20: 用第 5-20 帧输出
t = 24: 用第 9-24 帧输出
t = 28: 用第 13-28 帧输出
```

滑动窗口 chunk + 跨帧/跨 chunk 缓存复用 + 轻量视觉 token 压缩

---

如何在不等完整 chunk 的情况下，更早、更稳定地给出识别结果？

**流式识别逻辑 / 早期识别 / 滑动窗口策略 / 增量更新 / 稳定输出机制**。

持续接收帧
维护滑动窗口
窗口未满时也输出 early prediction
窗口满后输出 stable prediction
每来少量新帧就更新一次



先降低第一次识别结果延迟，给出early recognition 

原来：

```
等 16 帧才输出
```

可以改成：

```
攒到 4 帧先输出粗略候选
攒到 8 帧输出较可信结果
攒到 16 帧输出稳定结果
```

例如：

```
4 frames: top-5 candidate
8 frames: top-3 candidate
16 frames: top-1 stable result
```

然后考虑滑动窗口策略，并进行高频更新

就是复用已有的frame，结合新的frame和之前攒的一部分frame再做推理。类似token cache

然后可以有多个chunk的大小统计，维护多个chunk窗口

短窗口：8 帧，看快速动作
中窗口：16 帧，看标准动作
长窗口：24 帧，看复杂动作

输出时做融合：

```
短窗口负责低延迟
长窗口负责稳定性
```

最终结果可以是：

```
score = 0.5 * medium_score + 0.3 * short_score + 0.2 * long_score
```

最后一点是关于动作结束边界的判断，这个我也不大懂，可能需要额外的模型判断或者是视觉模型自己判断吧，然后结束的时候直接清空缓存吧